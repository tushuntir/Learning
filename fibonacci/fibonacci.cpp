#include <iostream>
#include <string>
using namespace std;

int main() {
    string sentence;
    cout << "Enter something: ";
    getline(cin, sentence);
    for (char letter : sentence) {
        int count;
        letter = 
        letter += count;
    }
}